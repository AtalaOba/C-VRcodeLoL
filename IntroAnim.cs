using UnityEngine;
using System.Collections;

public class IntroAnim : MonoBehaviour {
	
	/*// Update is called once per frame
	void Update () {
		if (Input.GetTouch = true) {
			GetComponent<Animation>().Play("IntroCanvas");
		}
	}
*/
}

